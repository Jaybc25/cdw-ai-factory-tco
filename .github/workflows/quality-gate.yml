name: AI Factory quality gate

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - "src/**"
      - "data/model_*.json"
      - "data/canonical_models.json"
      - "tests/**"
      - "scripts/extract_tco_workbook_snapshot.py"
      - "scripts/run_tco_parity.mjs"
      - "scripts/validate_pricing_registry.mjs"
      - "scripts/validate_model_registry.mjs"
      - "scripts/validate_model_tranche_1.mjs"
      - "scripts/validate_model_sizing_methodology.mjs"
      - "scripts/validate_model_advisor_semantics.mjs"
      - "scripts/validate_model_activation_readiness.mjs"
      - "scripts/validate_staged_model_runtime.mjs"
      - "scripts/validate_model_advisor_shadow_calibration.mjs"
      - "scripts/validate_model_activation_contract.mjs"
      - "scripts/validate_tco_handoff.mjs"
      - "scripts/validate_best_value_gpuaas.mjs"
      - "scripts/validate_best_value_gpuaas_ui.mjs"
      - "scripts/validate_global_reset.mjs"
      - "docs/reverse-tco-model-v1.xlsx"
      - "package.json"
      - "package-lock.json"
      - "playwright.config.js"
      - ".github/workflows/quality-gate.yml"
  pull_request:
    paths:
      - "src/**"
      - "data/model_*.json"
      - "data/canonical_models.json"
      - "tests/**"
      - "scripts/extract_tco_workbook_snapshot.py"
      - "scripts/run_tco_parity.mjs"
      - "scripts/validate_pricing_registry.mjs"
      - "scripts/validate_model_registry.mjs"
      - "scripts/validate_model_tranche_1.mjs"
      - "scripts/validate_model_sizing_methodology.mjs"
      - "scripts/validate_model_advisor_semantics.mjs"
      - "scripts/validate_model_activation_readiness.mjs"
      - "scripts/validate_staged_model_runtime.mjs"
      - "scripts/validate_model_advisor_shadow_calibration.mjs"
      - "scripts/validate_model_activation_contract.mjs"
      - "scripts/validate_tco_handoff.mjs"
      - "scripts/validate_best_value_gpuaas.mjs"
      - "scripts/validate_best_value_gpuaas_ui.mjs"
      - "scripts/validate_global_reset.mjs"
      - "docs/reverse-tco-model-v1.xlsx"
      - "package.json"
      - "package-lock.json"
      - "playwright.config.js"
      - ".github/workflows/quality-gate.yml"

permissions:
  contents: read

jobs:
  source-and-workbook:
    name: Build + TCO Excel parity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install application dependencies
        run: npm ci

      - name: Production build
        run: npm run build

      - name: Validate shared pricing registry
        run: node scripts/validate_pricing_registry.mjs

      - name: Validate shared model registry
        run: node scripts/validate_model_registry.mjs

      - name: Validate staged modern model tranche
        run: node scripts/validate_model_tranche_1.mjs

      - name: Validate architecture-aware model sizing methodology
        run: node scripts/validate_model_sizing_methodology.mjs

      - name: Validate Model Advisor semantic calibration
        run: node scripts/validate_model_advisor_semantics.mjs

      - name: Validate staged model activation readiness
        run: node scripts/validate_model_activation_readiness.mjs

      - name: Validate staged runtime semantics
        run: node scripts/validate_staged_model_runtime.mjs

      - name: Validate expanded Model Advisor shadow calibration
        run: node scripts/validate_model_advisor_shadow_calibration.mjs

      - name: Validate six-model activation contract
        run: node scripts/validate_model_activation_contract.mjs

      - name: Validate TCO GPU Sizing handoff defaults
        run: node scripts/validate_tco_handoff.mjs

      - name: Validate Best-Value GPUaaS ranking contract
        run: node scripts/validate_best_value_gpuaas.mjs

      - name: Validate Best-Value GPUaaS UI contract
        run: node scripts/validate_best_value_gpuaas_ui.mjs

      - name: Validate Global Reset contract
        run: node scripts/validate_global_reset.mjs

      - name: Validate SafeImageInput resource bounds
        run: node tests/safe-image-input.cjs

      - name: Validate Combined Summary presentation contract
        run: node tests/combined-summary-presentation.mjs

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Extract checked-in TCO workbook snapshot
        run: python3 scripts/extract_tco_workbook_snapshot.py

      - name: Verify final audit workbook structure
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          p = Path("tests/tco-parity/workbook_snapshot.json")
          data = json.loads(p.read_text())
          names = [s["name"] for s in data["sheets"]]
          required = {"Inputs", "Engine", "Results", "Validation", "EngineRegression", "Crossover"}
          missing = sorted(required - set(names))
          if missing:
            raise SystemExit(f"TCO workbook missing required audit sheets: {missing}. Present: {names}")
          print("Workbook sheets:", ", ".join(names))
          PY

      - name: Run Excel-to-JS parity gate
        run: node scripts/run_tco_parity.mjs

      - name: Upload workbook snapshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tco-workbook-snapshot
          path: tests/tco-parity/workbook_snapshot.json
          if-no-files-found: error
          retention-days: 14

  branch-handoff-e2e:
    name: Auth-bypassed build browser regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install application dependencies
        run: npm ci

      - name: Install Playwright test runner
        run: npm install --no-save --no-package-lock @playwright/test

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Build current PR source
        env:
          VITE_SUPABASE_URL: https://example.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: local-pr-browser-placeholder
          VITE_E2E_AUTH_BYPASS: "true"
        run: npm run build

      - name: Start local production preview
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 > /tmp/tco-preview.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/tco >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/tco-preview.log
          exit 1

      - name: Run full browser regression suite against the auth-bypassed build
        env:
          BASE_URL: http://127.0.0.1:4173
          E2E_AUTH_BYPASS: "true"
        run: npx playwright test tests/e2e

  live-smoke:
    name: Live Vercel browser regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install application dependencies
        run: npm ci

      - name: Install Playwright test runner
        run: npm install --no-save --no-package-lock @playwright/test

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run complete live regression suite
        env:
          BASE_URL: https://cdw-ai-factory-tco.vercel.app
        run: npx playwright test tests/e2e

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore
          retention-days: 14
