name: Temporary growth selection patch
on:
  push:
    branches:
      - gpu-sizing-growth-selection
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Diagnose
        run: echo "workflow available"
