name: ROI PDF layout experiment

on:
  push:
    branches:
      - experiment/roi-pdf-layout
    paths:
      - '.github/workflows/roi-pdf-layout-experiment.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-render:
    runs-on: ubuntu-latest
    env:
      VITE_SUPABASE_URL: https://example.supabase.co
      VITE_SUPABASE_PUBLISHABLE_KEY: experiment-only-placeholder
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experiment/roi-pdf-layout
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply branch-only ROI PDF layout changes
        run: python3 scripts/apply_roi_pdf_layout_experiment.py
      - name: Production build
        run: npm run build
      - name: Install Playwright
        run: npm install --no-save --no-package-lock @playwright/test
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Start local preview
        run: |
          npm run preview -- --host 127.0.0.1 > /tmp/roi-preview.log 2>&1 &
          for i in {1..30}; do
            curl -fsS http://127.0.0.1:4173/roi >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/roi-preview.log
          exit 1
      - name: Render default-scenario ROI experiment PDF
        run: node scripts/render_roi_report_experiment.mjs artifacts/roi-report-experiment.pdf
      - name: Upload comparison artifact
        uses: actions/upload-artifact@v4
        with:
          name: roi-report-layout-experiment
          path: artifacts/roi-report-experiment.pdf
          if-no-files-found: error
          retention-days: 14
      - name: Commit branch-only layout changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/RoiCalculator.jsx
          git diff --cached --check
          git diff --cached --quiet && exit 0
          git commit -m "experiment: improve ROI PDF layout"
          git push origin HEAD:experiment/roi-pdf-layout
