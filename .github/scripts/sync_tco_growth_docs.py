from pathlib import Path

# 1) Durable current-state document
p = Path('docs/MODEL_MODERNIZATION_CURRENT_STATE.md')
s = p.read_text()
s = s.replace('_Last updated: 2026-09-07 after PR #32_', '_Last updated: 2026-09-07 through PR #37 preview work_')
anchor = '''## Source-of-truth files\n'''
section = '''## TCO growth and cloud unit-price assumptions\n\nThe post-model-modernization TCO workstream keeps three separate concepts explicit:\n\n1. **Technical production design selection:** GPU Sizing owns the technical class/count. The node-rounded Recommended design remains the default; an existing Higher-growth alternative can be explicitly selected and is passed unchanged to TCO with `sizingBasis` provenance. TCO does not independently choose or re-size the technical design.\n2. **Workload/consumption growth:** the existing TCO annual growth input represents increased workload consumption over time. It is not a cloud provider price-escalation assumption.\n3. **Cloud GPU unit-price trend:** current production economics use a 0%/year unit-price trend, meaning the current cloud GPU $/GPU-hr rate is held constant across the selected analysis horizon while workload consumption may grow separately.\n\nPR #36 made the constant-unit-price assumption explicit in the calculator, report, and audit trail without changing TCO formulas, rates, defaults, or provider behavior.\n\nPR #37 is a preview-only sensitivity experiment for a possible future cloud GPU unit-price trend input. The preview control defaults to 0%/year and spans -20% to +20% per year in 5 percentage-point steps. The preview value is deliberately isolated from persistence, autosave, `run()` inputs, My Summary snapshots, and report economics. Moving the preview control must not change production TCO results.\n\nDo not wire the preview into economics until a separate methodology decision defines exactly which cloud cost components are affected, how annual compounding is applied, how the trend interacts with workload growth, whether spend-basis and workload-basis modes should behave identically, and what new parity/regression coverage is required. Capacity-ramp modeling remains a separate deferred concept and must not be conflated with unit-price trend sensitivity.\n\n'''
if section not in s:
    if anchor not in s:
        raise SystemExit('current-state source-of-truth anchor not found')
    s = s.replace(anchor, section + anchor, 1)
p.write_text(s)

# 2) Project brief addendum
p = Path('AiFactoryProjectBrief.md')
s = p.read_text()
anchor = '## September 5, 2026 Current-State Addendum\n'
addendum = '''## September 7, 2026 TCO Growth and Cloud Pricing Addendum\n\nThis addendum records the TCO/GPU Sizing modernization work tracked in issue #34 and supersedes older ambiguity around growth versus cloud unit-price movement.\n\n### GPU Sizing production-design selection\n\n- PR #35 keeps the node-rounded Recommended configuration as the default production design and allows an explicit Higher-growth alternative to be selected for downstream TCO analysis.\n- The Recommended and Higher-growth result cards themselves are the selection controls. The selected card is visibly marked `Selected for TCO`; Minimum Technical and Lower-Cost remain informational.\n- GPU Sizing remains the technical authority. TCO receives the exact selected GPU class/count plus `sizingBasis=recommended|higher-growth` provenance and does not independently choose or re-size the design.\n- Changing the sizing result resets the downstream choice to Recommended so a prior growth preference cannot silently carry into a materially different workload.\n- PR #35 merged as `465c77938a811dec6f08e353664ba87897c540ec` after the exact-head permanent quality gate passed. The direct-card selection interaction was subsequently confirmed working in production.\n\n### Cloud pricing assumption transparency\n\n- PR #36 made the existing cloud-pricing treatment explicit: current cloud GPU unit rates are held constant across the selected analysis horizon.\n- The annual TCO growth input represents increased workload/consumption, not provider price inflation or deflation.\n- The disclosure appears in the calculator, generated report, and calculation/audit trail. No TCO formulas, provider rates, growth defaults, provider-selection logic, GPU Sizing handoff semantics, or on-prem economics changed.\n- PR #36 merged as `05111bbedf4a9176d2ef3ae72ec495be282de894` after the exact-head permanent quality gate passed.\n\n### Cloud GPU unit-price trend preview\n\n- PR #37 is intentionally preview-only and remains subject to review before merge. It adds an interactive Cloud GPU unit-price trend control with default 0%/year, range -20% to +20% per year, and 5 percentage-point steps.\n- The preview value is deliberately not persisted, autosaved, passed into the TCO engine, included in My Summary state, or reflected in report economics. Production math therefore remains the same 0%/year unit-price treatment documented by PR #36.\n- Workload growth remains a separate production variable. Capacity-ramp modeling remains a separate deferred concept.\n- Any future wiring of cloud unit-price trend into economics requires a separate methodology approval defining affected cost components, compounding, interaction with workload growth, treatment by planning mode, and new parity/regression expectations.\n\n### Documentation and assurance discipline\n\n- `CHANGELOG.md` records all three workstreams chronologically.\n- `docs/MODEL_MODERNIZATION_CURRENT_STATE.md` now records the durable ownership and growth/unit-price contract because it governs the Model Advisor -> GPU Sizing -> TCO chain.\n- `MaintenanceRunbook.md` now contains the operating rule for reviewing any future cloud unit-price trend methodology.\n- The README checkpoint is refreshed so the repository landing page does not stop at PR #32.\n\n'''
if addendum not in s:
    if anchor not in s:
        raise SystemExit('project brief addendum anchor not found')
    s = s.replace(anchor, addendum + anchor, 1)
p.write_text(s)

# 3) Maintenance runbook
p = Path('MaintenanceRunbook.md')
s = p.read_text()
anchor = '## 5. Model-catalog refresh procedure\n'
section = '''### 4.5 TCO workload growth vs. cloud unit-price trend governance\n\nKeep these variables conceptually and operationally separate:\n\n- **Workload growth** is the production TCO assumption for increased consumption over time.\n- **Cloud GPU unit-price trend** is currently 0%/year in production economics, so the current cloud $/GPU-hr reference rate is held constant across the horizon.\n- The PR #37 slider is preview-only. Its UI value must not be persisted, autosaved, passed to the TCO engine, included in report economics, or treated as an approved methodology input.\n\nBefore any future unit-price trend is wired into production economics:\n\n1. Define which cloud cost components the trend applies to: GPU compute only, software uplift, storage, egress, or another explicitly scoped subset.\n2. Define annual compounding and the exact relationship to the separate workload-growth assumption.\n3. Decide whether Existing Cloud Spend and Workload Requirement modes use the same unit-price treatment or require mode-specific rules.\n4. Decide whether user-entered/custom cloud rates trend from the entered Year-1 rate or remain fixed unless explicitly opted in.\n5. Decide how Best-Value GPUaaS provider ranking should use the trend, if at all.\n6. Update the companion workbook/reference implementation if the production TCO engine changes in a way covered by Excel-to-JavaScript parity.\n7. Add source-level tests and browser regressions proving 0% reproduces the current baseline and positive/negative trends compound exactly as approved.\n8. Re-run the full permanent quality gate and perform representative live checks before calling the methodology production-ready.\n9. Update `CHANGELOG.md`, `AiFactoryProjectBrief.md`, the relevant current-state methodology document, and this runbook.\n\nDo not mix this work with future capacity-ramp modeling. Purchased future headroom versus current utilized workload is a separate modeling question.\n\n'''
if section not in s:
    if anchor not in s:
        raise SystemExit('runbook model-catalog anchor not found')
    s = s.replace(anchor, section + anchor, 1)
p.write_text(s)

# 4) README landing-page current state
p = Path('README.md')
s = p.read_text()
old = '''**Current unreleased code checkpoint before this documentation catch-up:** `main` at `4f74f97dd2bb06f762fb09e0b845f772a0435c63` on September 7, 2026. Post-2026.09.1 work includes curated My Summary consistency handling, Best-Value GPUaaS, Global Reset, home account/My Summary access, the Phase 1 authenticated front door, auth-aware regression architecture, provider commercial-eligibility gating, GPU Sizing print/PDF hierarchy refinement, and the completed first model-modernization tranche through PR #32. These changes remain `Unreleased` until a new validated tag is created.\n'''
new = '''**Current unreleased production checkpoint before this documentation catch-up:** `main` at `05111bbedf4a9176d2ef3ae72ec495be282de894` on September 7, 2026, the merge of PR #36. Post-2026.09.1 work includes curated My Summary consistency handling, Best-Value GPUaaS, Global Reset, home account/My Summary access, the Phase 1 authenticated front door, auth-aware regression architecture, provider commercial-eligibility gating, GPU Sizing print/PDF hierarchy refinement, the completed model-modernization tranche through PR #32, direct Recommended/Higher-growth production-design selection through PR #35, and explicit TCO cloud unit-price assumption disclosure through PR #36. Draft PR #37 is a preview-only cloud GPU unit-price trend sensitivity UI and does not change production economics. These changes remain `Unreleased` until a new validated tag is created.\n'''
if old in s:
    s = s.replace(old, new, 1)
elif new not in s:
    raise SystemExit('README checkpoint anchor not found')

anchor = '## Current model-catalog state\n'
section = '''## Current TCO growth/pricing contract\n\nGPU Sizing's node-rounded Recommended production design remains the default, with an explicit Higher-growth alternative selectable for TCO when available. TCO receives the exact selected class/count plus sizing-basis provenance and remains the economics layer rather than re-sizing the workload.\n\nCurrent TCO production economics treat **workload growth** and **cloud GPU unit price** separately: workload growth may increase consumption over time, while the current cloud GPU $/GPU-hr rate is held constant at **0% annual unit-price trend** across the analysis horizon. Draft PR #37 exposes a -20% to +20% preview control only for UX/methodology evaluation; moving it does not alter persisted state, autosave, reports, or TCO results.\n\n'''
if section not in s:
    if anchor not in s:
        raise SystemExit('README model-catalog anchor not found')
    s = s.replace(anchor, section + anchor, 1)
p.write_text(s)
